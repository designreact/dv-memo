/*
 * App includes a basic reducer to enable inclusion into ../reducers
 * Stores dynamic application config e.g. API endpoints
 */

const app = (state = {}) => {
  return state
}

export default app
