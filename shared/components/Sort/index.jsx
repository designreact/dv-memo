if (process.env.CLIENT) {
  require('./_styles.scss')
}

import React from 'react'
// import PropTypes from 'prop-types'

const Sort = () => {
  return (
    <div className="sort" />
  )
}

export default Sort
